# Copia este archivo como ".env" (en la misma carpeta frontend) si quieres apuntar el
# desarrollo local a otro backend. Si no lo creas, se usa http://localhost:4000/api por defecto.

VITE_API_URL=http://localhost:4000/api
